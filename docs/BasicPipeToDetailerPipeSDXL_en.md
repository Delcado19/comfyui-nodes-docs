# Documentation
- Class name: BasicPipeToDetailerPipeSDXL
- Category: ImpactPack/Pipe
- Output node: False
- Repo Ref: https://github.com/ltdrdata/ComfyUI-Impact-Pack.git

The BasicPipeToDetailerPipeSDXL node converts a basic pipeline structure into a more detailed form suitable for advanced processing stages. It enhances the pipeline's capabilities for tasks like segmentation and refinement by integrating components such as bbox_detector, LoRA, and Wildcard selection.

# Input types
## Required
- base_basic_pipe
    - The base_basic_pipe parameter is required, as it provides the foundational pipeline structure the node needs to operate. This is a critical input that directly affects the node's ability to process and transform data.
    - Comfy dtype: BASIC_PIPE
    - Python dtype: Tuple[Any, ...]
- refiner_basic_pipe
    - The refiner_basic_pipe parameter refines the base pipeline, providing an additional layer of processing capability. It is a key component for improving the detail and accuracy of the pipeline's output.
    - Comfy dtype: BASIC_PIPE
    - Python dtype: Tuple[Any, ...]
- bbox_detector
    - The bbox_detector is a critical component for identifying and locating regions of interest in the data. Its role is essential for enabling the node to perform spatial analysis and object detection tasks.
    - Comfy dtype: BBOX_DETECTOR
    - Python dtype: Any
- wildcard
    - The wildcard parameter allows dynamic input patterns, enhancing the node's flexibility. It is significant for adapting to various data formats and processing requirements.
    - Comfy dtype: STRING
    - Python dtype: str
## Optional
- sam_model_opt
    - The optional sam_model_opt parameter can specify a model for semantic annotation, adding depth to the node's analytical capabilities.
    - Comfy dtype: SAM_MODEL
    - Python dtype: Optional[Any]
- segm_detector_opt
    - The provided segm_detector_opt parameter enables the node to perform segmentation tasks, contributing to more detailed analysis of the data.
    - Comfy dtype: SEGM_DETECTOR
    - Python dtype: Optional[Any]
- detailer_hook
    - The detailer_hook parameter is an optional feature that allows customization of the node's processing pipeline to meet specific use cases or requirements.
    - Comfy dtype: DETAILER_HOOK
    - Python dtype: Optional[Any]

# Output types
- detailer_pipe
    - The detailer_pipe output is the refined and detailed pipeline structure generated by the node. It is important because it lays the foundation for further advanced processing and analysis.
    - Comfy dtype: DETAILER_PIPE
    - Python dtype: Tuple[Any, ...]

# Usage tips
- Infra type: CPU

# Source code
```
class BasicPipeToDetailerPipeSDXL:

    @classmethod
    def INPUT_TYPES(s):
        return {'required': {'base_basic_pipe': ('BASIC_PIPE',), 'refiner_basic_pipe': ('BASIC_PIPE',), 'bbox_detector': ('BBOX_DETECTOR',), 'wildcard': ('STRING', {'multiline': True, 'dynamicPrompts': False}), 'Select to add LoRA': (['Select the LoRA to add to the text'] + folder_paths.get_filename_list('loras'),), 'Select to add Wildcard': (['Select the Wildcard to add to the text'],)}, 'optional': {'sam_model_opt': ('SAM_MODEL',), 'segm_detector_opt': ('SEGM_DETECTOR',), 'detailer_hook': ('DETAILER_HOOK',)}}
    RETURN_TYPES = ('DETAILER_PIPE',)
    RETURN_NAMES = ('detailer_pipe',)
    FUNCTION = 'doit'
    CATEGORY = 'ImpactPack/Pipe'

    def doit(self, *args, **kwargs):
        base_basic_pipe = kwargs['base_basic_pipe']
        refiner_basic_pipe = kwargs['refiner_basic_pipe']
        bbox_detector = kwargs['bbox_detector']
        wildcard = kwargs['wildcard']
        sam_model_opt = kwargs.get('sam_model_opt', None)
        segm_detector_opt = kwargs.get('segm_detector_opt', None)
        detailer_hook = kwargs.get('detailer_hook', None)
        (model, clip, vae, positive, negative) = base_basic_pipe
        (refiner_model, refiner_clip, refiner_vae, refiner_positive, refiner_negative) = refiner_basic_pipe
        pipe = (model, clip, vae, positive, negative, wildcard, bbox_detector, segm_detector_opt, sam_model_opt, detailer_hook, refiner_model, refiner_clip, refiner_positive, refiner_negative)
        return (pipe,)
```