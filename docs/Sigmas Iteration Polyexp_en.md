# Documentation
- Class name: Sigmas Iteration Polyexp
- Category: RES4LYF/schedulers
- Output node: False
- Repo Ref: https://github.com/ClownsharkBatwing/RES4LYF

Sigmas Iteration Polyexp is a ComfyUI node registered by `custom_nodes.RES4LYF`. The live metadata did not provide a longer description.

# Input types
## Required
- steps_up
    - The steps_up input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- steps_down
    - The steps_down input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- rho_up
    - The rho_up input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- rho_down
    - The rho_down input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- s_min_start
    - The s_min_start input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- s_max
    - The s_max input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- s_min_end
    - The s_min_end input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
## Optional
- momentums
    - The momentums input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- sigmas
    - The sigmas input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object

# Output types
- momentums
    - The momentums output is produced by this node.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- sigmas
    - The sigmas output is produced by this node.
    - Comfy dtype: SIGMAS
    - Python dtype: object

# Usage tips
- Infra type: unknown

# Source code
[View source repository](https://github.com/ClownsharkBatwing/RES4LYF)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
