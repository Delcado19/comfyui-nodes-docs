# Documentation
- Class name: iToolsRegexNode
- Category: iTools
- Output node: False
- Repo Ref: https://github.com/MohammadAboulEla/ComfyUI-iTools

Uses Regex to find, match, or modify text. Returns matches if no replacement is set, otherwise, replaces matches or non-matches as specified.

# Input types
## Required
- text_in
    - The text_in input is used by this node during execution.
    - Comfy dtype: STRING
    - Python dtype: str
- regex_pattern
    - The regex_pattern input is used by this node during execution.
    - Comfy dtype: STRING
    - Python dtype: str
- pattern_picker
    - The pattern_picker input is used by this node during execution.
    - Comfy dtype: COMBO[STRING]
    - Python dtype: str
- replace_match
    - The replace_match input is used by this node during execution.
    - Comfy dtype: STRING
    - Python dtype: str
- replace_non_match
    - The replace_non_match input is used by this node during execution.
    - Comfy dtype: STRING
    - Python dtype: str

# Output types
- match
    - The match output is produced by this node.
    - Comfy dtype: STRING
    - Python dtype: str

# Usage tips
- Infra type: unknown

# Source code
[View source repository](https://github.com/MohammadAboulEla/ComfyUI-iTools)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
