# Documentation
- Class name: LayerMask: SegformerClothesPipelineLoader
- Category: 😺dzNodes/LayerMask
- Output node: False
- Repo Ref: https://github.com/chflame163/ComfyUI_LayerStyle

A set of nodes for ComfyUI it generate image like Adobe Photoshop's Layer Style. the Drop Shadow is first completed node, and follow-up work is in progress.

# Input types
## Required
- model
    - The model input is used by this node during execution.
    - Comfy dtype: COMBO[STRING]
    - Python dtype: str
- face
    - The face input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- hair
    - The hair input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- hat
    - The hat input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- sunglass
    - The sunglass input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- left_arm
    - The left_arm input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- right_arm
    - The right_arm input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- left_leg
    - The left_leg input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- right_leg
    - The right_leg input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- left_shoe
    - The left_shoe input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- right_shoe
    - The right_shoe input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- upper_clothes
    - The upper_clothes input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- skirt
    - The skirt input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- pants
    - The pants input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- dress
    - The dress input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- belt
    - The belt input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- bag
    - The bag input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- scarf
    - The scarf input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool

# Output types
- segformer_pipeline
    - The segformer_pipeline output is produced by this node.
    - Comfy dtype: SegPipeline
    - Python dtype: object

# Usage tips
- Infra type: unknown

# Source code
[View source repository](https://github.com/chflame163/ComfyUI_LayerStyle)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
