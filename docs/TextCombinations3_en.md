# Documentation
- Class name: TextCombinations3
- Category: Mikey/Text
- Output node: False
- Repo Ref: https://github.com/bash-j/mikey_nodes

The TextCombinations3 node is designed to generate a set of text operations that combine input text in various ways. It provides a method to mix and transform text strings based on predefined operations and a specified delimiter. The node's functionality is focused on creating combinations of text inputs to produce diverse outputs, which is very useful for text analysis or data preprocessing tasks.

# Input types
## Required
- text1
    - The 'text1' parameter is a string representing the first input text. It plays a key role in the node's operations because it is one of the main sources of text data that will be combined and manipulated to generate the final output.
    - Comfy dtype: STRING
    - Python dtype: str
- text2
    - The 'text2' parameter is another string input that complements 'text1'. It is essential for creating diverse text combinations and is used together with 'text1' to achieve the node's purpose of generating mixed text outputs.
    - Comfy dtype: STRING
    - Python dtype: str
- text3
    - The 'text3' parameter is the node's third input text string. It is important for expanding the range of possible text combinations and ensuring the node can produce a wide variety of outputs.
    - Comfy dtype: STRING
    - Python dtype: str
- operation
    - The 'operation' parameter defines the specific text combination to be performed. It is a critical determinant of the node's execution, as it decides how the input texts will be mixed to create the desired output.
    - Comfy dtype: STRING
    - Python dtype: str
## Optional
- delimiter
    - The 'delimiter' parameter specifies the character or string used to separate the combined text components. It is important for defining the format of the final output and ensuring the mixed text is presented in a coherent manner.
    - Comfy dtype: STRING
    - Python dtype: str
- use_seed
    - The 'use_seed' parameter is a boolean flag indicating whether a seed value should be used to select an operation from a predefined list. It affects the randomness and repeatability of the text combination process.
    - Comfy dtype: STRING
    - Python dtype: str
- seed
    - The 'seed' parameter is an integer that serves as the random seed for operation selection when 'use_seed' is set to true. It ensures a consistent starting point for generating text combinations, which is essential for obtaining repeatable results.
    - Comfy dtype: INT
    - Python dtype: int

# Output types
- output1
    - The 'output1' parameter represents the first mixed text output generated by the node. This is an important result of the node's operation, demonstrating the combination and transformation of the input texts.
    - Comfy dtype: STRING
    - Python dtype: str
- output2
    - The 'output2' parameter is the node's second mixed text output. It reflects another variation of the input texts, showcasing the node's ability to produce multiple distinct outputs.
    - Comfy dtype: STRING
    - Python dtype: str
- output3
    - The 'output3' parameter represents the third mixed text output. It is another example of how the node manipulates and combines text inputs to produce a diverse set of results.
    - Comfy dtype: STRING
    - Python dtype: str

# Usage tips
- Infra type: CPU

# Source code
[View source repository on GitHub](https://github.com/bash-j/mikey_nodes)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
