# Documentation
- Class name: LatentPhaseMagnitudePower
- Category: RES4LYF/latents
- Output node: False
- Repo Ref: https://github.com/ClownsharkBatwing/RES4LYF

LatentPhaseMagnitudePower is a ComfyUI node registered by `custom_nodes.RES4LYF`. The live metadata did not provide a longer description.

# Input types
## Required
- latent_0_batch
    - The latent_0_batch input is used by this node during execution.
    - Comfy dtype: LATENT
    - Python dtype: dict
- phase_luminosity
    - The phase_luminosity input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- phase_cyan_red
    - The phase_cyan_red input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- phase_lime_purple
    - The phase_lime_purple input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- phase_pattern_structure
    - The phase_pattern_structure input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- magnitude_luminosity
    - The magnitude_luminosity input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- magnitude_cyan_red
    - The magnitude_cyan_red input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- magnitude_lime_purple
    - The magnitude_lime_purple input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- magnitude_pattern_structure
    - The magnitude_pattern_structure input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- latent_0_normal
    - The latent_0_normal input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- latent_out_normal
    - The latent_out_normal input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
## Optional
- phase_luminositys
    - The phase_luminositys input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- phase_cyan_reds
    - The phase_cyan_reds input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- phase_lime_purples
    - The phase_lime_purples input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- phase_pattern_structures
    - The phase_pattern_structures input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- magnitude_luminositys
    - The magnitude_luminositys input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- magnitude_cyan_reds
    - The magnitude_cyan_reds input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- magnitude_lime_purples
    - The magnitude_lime_purples input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- magnitude_pattern_structures
    - The magnitude_pattern_structures input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object

# Output types
- LATENT
    - The LATENT output is produced by this node.
    - Comfy dtype: LATENT
    - Python dtype: dict

# Usage tips
- Infra type: unknown

# Source code
[View source repository](https://github.com/ClownsharkBatwing/RES4LYF)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
