# Documentation
- Class name: LatentNoiseList
- Category: RES4LYF/noise
- Output node: False
- Repo Ref: https://github.com/ClownsharkBatwing/RES4LYF

LatentNoiseList is a ComfyUI node registered by `custom_nodes.RES4LYF`. The live metadata did not provide a longer description.

# Input types
## Required
- latent
    - The latent input is used by this node during execution.
    - Comfy dtype: LATENT
    - Python dtype: dict
- alpha
    - The alpha input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- k_flip
    - The k_flip input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- steps
    - The steps input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- seed
    - The seed input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
## Optional
- alphas
    - The alphas input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object
- ks
    - The ks input is used by this node during execution.
    - Comfy dtype: SIGMAS
    - Python dtype: object

# Output types
- latent_list
    - The latent_list output is produced by this node.
    - Comfy dtype: LATENT
    - Python dtype: dict

# Usage tips
- Infra type: unknown

# Source code
[View source repository](https://github.com/ClownsharkBatwing/RES4LYF)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
