# Documentation
- Class name: SEGSConcat
- Category: ImpactPack/Util
- Output node: False
- Repo Ref: https://github.com/ltdrdata/ComfyUI-Impact-Pack.git

The SEGSConcat node is designed to merge multiple segmentations (SEGS) into a single unified segmentation. It efficiently combines segmentation data, ensuring the resulting segmentation is coherent and maintains the integrity of the original data shape.

# Input types
## Required
- segs1
    - The 'segs1' parameter is the collection of segmentations to be concatenated. It plays a key role in defining the input data for the node, which will be merged into a single segmentation output.
    - Comfy dtype: SEGS
    - Python dtype: List[Tuple[Tuple[int, int], List[Any]]]

# Output types
- output
    - The 'output' parameter represents the concatenated segmentation generated by the merge process. It is important as it is the primary output of the node, containing the merged segmentation data.
    - Comfy dtype: SEGS
    - Python dtype: Tuple[Tuple[int, int], List[Any]]

# Usage tips
- Infra type: CPU

# Source code
[View source repository on GitHub](https://github.com/ltdrdata/ComfyUI-Impact-Pack)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
