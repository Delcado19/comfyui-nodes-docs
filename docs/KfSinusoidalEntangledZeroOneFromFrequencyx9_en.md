# Documentation
- Class name: KfSinusoidalEntangledZeroOneFromFrequencyx9
- Category: CATEGORY
- Output node: False
- Repo Ref: https://github.com/dmarx/ComfyUI-Keyframed

The KfSinusoidalEntangledZeroOneFromFrequencyx9 node is designed to generate a set of nine sinusoidal curves that are entangled with each other, oscillating between zero and one. It uses the keyframed library to create smooth and continuous transitions. The purpose of this node is to provide a versatile tool for generating complex waveforms that can be used in various applications, such as animation, signal processing, or any scenario requiring sinusoidal patterns.

# Input types
## Required
- frequency
    - The frequency parameter determines the rate of oscillation of the sinusoidal curves. It is a key element in shaping the overall behavior of the waveform generated by the node. Changes in frequency will directly affect the periodicity and speed of the oscillation.
    - Comfy dtype: float
    - Python dtype: float

# Output types
- KEYFRAMED_CURVE
    - The output of the KfSinusoidalEntangledZeroOneFromFrequencyx9 node is a tuple of nine keyframed sinusoidal curves. Each curve represents a unique oscillation within the entangled waveform, as part of the overall complex pattern. These curves can be further manipulated or used in downstream processes.
    - Comfy dtype: COMBO[kf.SinusoidalCurve]
    - Python dtype: Tuple[kf.SinusoidalCurve, ...]

# Usage tips
- Infra type: CPU

# Source code
```
class KfSinusoidalEntangledZeroOneFromFrequencyx9(KfSinusoidalEntangledZeroOneFromFrequency):
    RETURN_TYPES = ('KEYFRAMED_CURVE',) * 9

    def main(self, frequency):
        return super().main(n=9, frequency=frequency)
```