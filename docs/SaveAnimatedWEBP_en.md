# Documentation
- Class name: SaveAnimatedWEBP
- Category: image/animation
- Output node: True
- Repo Ref: https://github.com/comfyanonymous/ComfyUI

The 'save_images' method of the SaveAnimatedWEBP node is designed to efficiently save a series of images as an animated WEBP file. It utilizes the PIL library to convert images into a format suitable for animation, allowing control over frame rate, lossless compression, and quality settings. This node plays a critical role in the final output stage of an image processing workflow, capable of creating animated visual effects from a series of static images.

# Input types
## Required
- images
    - The "images" parameter is crucial for the node, as it defines the input images to be saved as an animation. Each image constitutes a frame in the final output, influencing the entire animation sequence and its visual quality.
    - Comfy dtype: IMAGE
    - Python dtype: List[torch.Tensor]
- fps
    - The "fps" parameter determines the frame rate of the animation, which is a critical factor affecting the final output in terms of motion perception. It dictates the speed at which frames are displayed, influencing the overall smoothness of the animation.
    - Comfy dtype: FLOAT
    - Python dtype: float
- lossless
    - The "lossless" parameter is important for controlling the compression method used when saving images. When set to True, it ensures the saved animation maintains the highest quality without any loss of image data.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool
- quality
    - The "quality" parameter affects the balance between file size and image quality in the saved animation. Higher values yield better image quality but also increase file size, while lower values do the opposite.
    - Comfy dtype: INT
    - Python dtype: int
- method
    - The "method" parameter selects the compression algorithm used when creating the animated WEBP file. Different methods offer varying degrees of compression efficiency and speed, affecting the final file size and the time required to save the animation.
    - Comfy dtype: COMBO[default, fastest, slowest]
    - Python dtype: Literal['default', 'fastest', 'slowest']
## Optional
- filename_prefix
    - The "filename_prefix" parameter is very important, as it determines the naming convention for saved files, which is crucial for organizing and identifying outputs. It provides a base name that is then combined with a unique identifier to create the full filename.
    - Comfy dtype: STRING
    - Python dtype: str

# Output types
- ui
    - The 'ui' parameter in the output is a dictionary containing details of the saved images, including filenames and subfolders. It serves as a record of the output generated by the node, providing the user with a reference to the location and nature of the saved animation files.
    - Comfy dtype: DICT
    - Python dtype: Dict[str, Any]

# Usage tips
- Infra type: CPU

# Source code
[View source repository on GitHub](https://github.com/comfyanonymous/ComfyUI)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
