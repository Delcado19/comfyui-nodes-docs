# Documentation
- Class name: VRGDG_TrimFinalClip
- Category: VRGDG
- Output node: False
- Repo Ref: https://github.com/vrgamegirl19/comfyui-vrgamedevgirl

VRGDG_TrimFinalClip is a ComfyUI node registered by `custom_nodes.comfyui-vrgamedevgirl`. The live metadata did not provide a longer description.

# Input types
## Required
- trigger
    - The trigger input is used by this node during execution.
    - Comfy dtype: VHS_FILENAMES
    - Python dtype: object
- output_folder
    - The output_folder input is used by this node during execution.
    - Comfy dtype: STRING
    - Python dtype: str
- base_name
    - The base_name input is used by this node during execution.
    - Comfy dtype: STRING
    - Python dtype: str
- frames_per_scene
    - The frames_per_scene input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- audio_total_duration
    - The audio_total_duration input is used by this node during execution.
    - Comfy dtype: FLOAT
    - Python dtype: float
- index
    - The index input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- total_sets
    - The total_sets input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- fps
    - The fps input is used by this node during execution.
    - Comfy dtype: INT
    - Python dtype: int
- overwrite
    - The overwrite input is used by this node during execution.
    - Comfy dtype: BOOLEAN
    - Python dtype: bool

# Output types
- final_clip_path
    - The final_clip_path output is produced by this node.
    - Comfy dtype: STRING
    - Python dtype: str

# Usage tips
- Infra type: unknown

# Source code
[View source repository](https://github.com/vrgamegirl19/comfyui-vrgamedevgirl)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
