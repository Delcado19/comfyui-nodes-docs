# Documentation
- Class name: SaveAnimatedPNG
- Category: image/animation
- Output node: True
- Repo Ref: https://github.com/comfyanonymous/ComfyUI

The SaveAnimatedPNG node is designed to efficiently save a sequence of images as an animated PNG file. It takes into account frame rate and compression level to optimize the performance and storage requirements of the animation. This node plays a critical role in the final output stage of an image generation workflow, ensuring that the generated animation is both high-quality and efficiently packaged.

# Input types
## Required
- images
    - The 'images' parameter is crucial because it defines the sequence of images that will be compiled into the animated PNG. It directly affects the final output, determining the content and quality of the animation.
    - Comfy dtype: IMAGE
    - Python dtype: torch.Tensor
- fps
    - The 'fps' parameter determines the playback speed of the animation, measured in frames per second. It is a key factor in setting the rhythm of the animation and ensuring a smooth viewing experience.
    - Comfy dtype: FLOAT
    - Python dtype: float
- compress_level
    - The 'compress_level' parameter adjusts the compression level of the PNG file, which helps balance file size against image quality. This is important for optimizing storage and loading times.
    - Comfy dtype: INT
    - Python dtype: int
## Optional
- filename_prefix
    - The 'filename_prefix' parameter allows users to specify a custom prefix for the output file name, which is useful for organizing and identifying saved animations.
    - Comfy dtype: STRING
    - Python dtype: str
- prompt
    - The 'prompt' parameter is used to store additional context or descriptions related to the images being saved. It can provide valuable information for future reference or metadata purposes.
    - Comfy dtype: PROMPT
    - Python dtype: str
- extra_pnginfo
    - The 'extra_pnginfo' parameter allows additional metadata to be included in the PNG file, which can be used to embed custom data or annotations.
    - Comfy dtype: EXTRA_PNGINFO
    - Python dtype: Dict[str, str]

# Output types
- ui
    - The 'ui' parameter encapsulates the user interface elements generated by the node's operation. It includes details such as file names and subfolders, which are essential for users to locate and interact with the saved animations.
    - Comfy dtype: DICT
    - Python dtype: Dict[str, Any]

# Usage tips
- Infra type: CPU

# Source code
[View source repository on GitHub](https://github.com/comfyanonymous/ComfyUI)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
