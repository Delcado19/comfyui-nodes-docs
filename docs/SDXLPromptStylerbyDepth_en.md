# Documentation
- Class name: SDXLPromptStylerbyDepth
- Category: Style Prompts
- Output node: False
- Repo Ref: https://github.com/wolfden/ComfyUi_PromptStylers

The SDXLPromptStylerbyDepth node aims to enhance user experience through depth-based stylized prompts. It intelligently combines positive and negative text inputs along with the selected style, aiming to generate more engaging and targeted prompts. This node plays a crucial role in creating prompts that resonate with the target audience, thereby improving communication effectiveness.

# Input types
## Required
- text_positive
    - The text_positive parameter is critical as it provides the positive text content that will be styled by the node. It directly influences the tone and message of the final prompt and is a key component of the node's operation.
    - Comfy dtype: STRING
    - Python dtype: str
- text_negative
    - The text_negative parameter is important for providing negative text content that contrasts with the positive text. It helps shape the overall sentiment of the prompt and is a significant factor in the node's execution.
    - Comfy dtype: STRING
    - Python dtype: str
- style
    - The style parameter is critical as it determines the styling approach applied to the text. It dictates the selection of templates and the overall aesthetic of the generated prompt, making it a key element of the node's functionality.
    - Comfy dtype: STRING
    - Python dtype: str
- log_prompt
    - The log_prompt parameter is optional and controls whether the node should log prompt information during execution. This is useful for debugging and monitoring the node's behavior and does not affect the main functionality.
    - Comfy dtype: COMBO['No', 'Yes']
    - Python dtype: str

# Output types
- positive_prompt_text_g
    - The positive_prompt_text_g output represents the styled positive text processed by the node. It is a key result of the node's operation and holds significant importance for subsequent use in various applications.
    - Comfy dtype: STRING
    - Python dtype: str
- negative_prompt_text_g
    - The negative_prompt_text_g output represents the styled negative text generated by the node. It complements the positive text and is an important aspect of the node's output for creating balanced and nuanced prompts.
    - Comfy dtype: STRING
    - Python dtype: str

# Usage tips
- Infra type: CPU

# Source code
[View source repository on GitHub](https://github.com/wolfden/ComfyUi_PromptStylers)

*Source code is not embedded in this doc — browse the pack's repository at the link above.*
