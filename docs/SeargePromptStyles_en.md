# Documentation
- Class name: SeargePromptStyles
- Category: UI_INPUTS
- Output node: False
- Repo Ref: https://github.com/jobunk/SeargeSDXL.git

SeargePromptStyles is a node for managing and retrieving prompt styles. It serves as an interface for users to define and access prompt style configurations, ensuring prompts are formatted according to user requirements.

# Input types
## Required
- data
    - The 'data' parameter is a dictionary that holds the context of prompt styles. It is optional and can be used to provide initial data or settings that the node will use to determine the styling of prompts.
    - Comfy dtype: Dict[str, Any]
    - Python dtype: Dict[str, Any]

# Output types
- data
    - The output 'data' is the styled prompt data stream generated by the node based on input parameters and style configurations. It is important because it provides the final formatted prompt for use in subsequent processes.
    - Comfy dtype: SRG_DATA_STREAM
    - Python dtype: SRG_DATA_STREAM

# Usage tips
- Infra type: CPU

# Source code
```
class SeargePromptStyles:

    @classmethod
    def INPUT_TYPES(cls):
        return {'required': {}, 'optional': {'data': ('SRG_DATA_STREAM',)}}
    RETURN_TYPES = ('SRG_DATA_STREAM',)
    RETURN_NAMES = ('data',)
    FUNCTION = 'get'
    CATEGORY = UI.CATEGORY_UI_INPUTS

    @staticmethod
    def create_dict(example):
        return {UI.EXAMPLE: example}

    def get(self, data=None):
        if data is None:
            data = {}
        data[UI.S_PROMPT_STYLING] = self.create_dict('example')
        return (data,)
```